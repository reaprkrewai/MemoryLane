---
phase: 02-editor-tags
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/TagRow.tsx
  - src/components/TagPill.tsx
  - src/components/TagInput.tsx
  - src/components/TagAutocomplete.tsx
  - src/components/DeleteEntryDialog.tsx
  - src/components/EntryEditor.tsx
  - src/components/EntryListItem.tsx
autonomous: true
requirements:
  - TAG-01
  - TAG-02
  - TAG-03
  - TAG-04

must_haves:
  truths:
    - "User can type a tag name in the input below the editor and press Enter to create and assign it"
    - "Autocomplete dropdown shows existing tags with color swatch and usage count as user types"
    - "Each tag pill has a tinted background color from its assigned preset color"
    - "User can click a tag pill to open a color picker popover with 8 preset colors"
    - "User can remove a tag from an entry by clicking the x on its pill"
    - "Tags with 0 entries can be deleted (via tag management or autocomplete)"
    - "User can delete an entry via the sidebar with a confirmation dialog"
  artifacts:
    - path: "src/components/TagRow.tsx"
      provides: "Tag pills + tag input row below editor"
      min_lines: 20
    - path: "src/components/TagPill.tsx"
      provides: "Colored pill with remove button and color picker"
      min_lines: 25
    - path: "src/components/TagInput.tsx"
      provides: "Borderless input with autocomplete trigger"
      min_lines: 20
    - path: "src/components/TagAutocomplete.tsx"
      provides: "Dropdown with tag suggestions, usage counts, and create option"
      min_lines: 30
    - path: "src/components/DeleteEntryDialog.tsx"
      provides: "AlertDialog confirmation for entry deletion"
      min_lines: 20
  key_links:
    - from: "src/components/TagRow.tsx"
      to: "src/stores/tagStore.ts"
      via: "useTagStore for entry tags and CRUD"
      pattern: "useTagStore"
    - from: "src/components/TagAutocomplete.tsx"
      to: "src/stores/tagStore.ts"
      via: "useTagStore for all tags with usage counts"
      pattern: "useTagStore"
    - from: "src/components/DeleteEntryDialog.tsx"
      to: "src/stores/entryStore.ts"
      via: "useEntryStore.deleteEntry"
      pattern: "deleteEntry"
---

<objective>
Build the complete tag management system (tag input, autocomplete, pills with color picker, tag deletion) and the delete entry dialog. Wire both into the editor and sidebar.

Purpose: Complete the tag workflow so users can organize entries with colored tags, and enable entry deletion with proper confirmation UX.
Output: Tag row below editor with inline creation, autocomplete, color-coded pills, and color picker. Delete entry confirmation dialog accessible from sidebar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-editor-tags/02-CONTEXT.md
@.planning/phases/02-editor-tags/02-RESEARCH.md
@.planning/phases/02-editor-tags/02-UI-SPEC.md
@.planning/phases/02-editor-tags/02-01-SUMMARY.md

@src/stores/entryStore.ts
@src/stores/tagStore.ts
@src/components/EntryEditor.tsx
@src/components/EntryListItem.tsx
@src/styles/globals.css

<interfaces>
<!-- From tagStore.ts (created in Plan 01): -->
```typescript
export const TAG_COLORS = [
  '#EF4444', '#F97316', '#EAB308', '#22C55E',
  '#3B82F6', '#8B5CF6', '#EC4899', '#6B7280',
] as const

interface TagWithCount {
  id: string; name: string; color: string; created_at: number; usage_count: number;
}

export const useTagStore = create<TagState>((set, get) => ({
  tags: TagWithCount[],
  loadTags: () => Promise<void>,
  createTag: (name: string) => Promise<Tag>,
  deleteTag: (id: string) => Promise<void>,
  updateTagColor: (id: string, color: string) => Promise<void>,
  addTagToEntry: (entryId: string, tagId: string) => Promise<void>,
  removeTagFromEntry: (entryId: string, tagId: string) => Promise<void>,
  getEntryTags: (entryId: string) => Promise<TagWithCount[]>,
}))
```

<!-- From entryStore.ts (created in Plan 01): -->
```typescript
export const useEntryStore = create<EntryState>((set, get) => ({
  entries: Entry[],
  selectedEntryId: string | null,
  deleteEntry: (id: string) => Promise<void>,
  // ...
}))
```

<!-- shadcn components available: -->
<!-- src/components/ui/popover.tsx — Popover, PopoverTrigger, PopoverContent -->
<!-- src/components/ui/alert-dialog.tsx — AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogCancel, AlertDialogAction -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TagPill, TagInput, TagAutocomplete, and TagRow components</name>
  <files>
    src/components/TagPill.tsx,
    src/components/TagInput.tsx,
    src/components/TagAutocomplete.tsx,
    src/components/TagRow.tsx
  </files>
  <read_first>
    src/stores/tagStore.ts,
    src/stores/entryStore.ts,
    src/components/ui/popover.tsx,
    src/styles/globals.css,
    .planning/phases/02-editor-tags/02-UI-SPEC.md,
    .planning/phases/02-editor-tags/02-CONTEXT.md
  </read_first>
  <action>
**Step 1: Create `src/components/TagPill.tsx` per UI-SPEC Tag Row contract, D-09.**

Props: `tag: { id: string; name: string; color: string }`, `onRemove: () => void`, `onColorChange: (color: string) => void`.

Pill shape: `rounded-full`, inline-flex, `items-center`, `gap-1`, padding `py-[4px] px-3` (4px vertical / 12px horizontal per UI-SPEC).

Background: use CSS `color-mix(in srgb, ${tag.color} 15%, transparent)` via inline style: `style={{ backgroundColor: \`color-mix(in srgb, ${tag.color} 15%, transparent)\` }}`. Border: `style={{ borderColor: \`color-mix(in srgb, ${tag.color} 40%, transparent)\` }}` with `border` class.

Tag name text: `text-label` (12px), weight 400. Color derived from tag color — use the tag color directly for dark enough colors, or use `var(--color-text)` for light colors. Simplest approach: always use `var(--color-text)` for readability.

Remove button (x): Hidden by default, visible on pill hover. Use a `group` class on the pill wrapper and `opacity-0 group-hover:opacity-100` on the x button. Character: `X` icon from lucide-react at 14px, same color as text. On hover of the x itself: `text-destructive`. onClick: call `onRemove()`.

**Color picker popover (D-09):** Clicking the pill (not the x) opens a shadcn `Popover`. The PopoverContent shows 8 color swatches in a 4x2 grid (using `grid grid-cols-4 gap-1`). Each swatch is a 20x20px circle button (`w-5 h-5 rounded-full`) with `style={{ backgroundColor: color }}`. The currently selected color has a checkmark icon (lucide `Check`, 12px, white or dark depending on contrast). Clicking a swatch calls `onColorChange(color)` and the popover auto-closes (manage with a local `open` state).

Import `TAG_COLORS` from `../stores/tagStore` for the 8 preset colors.

**Step 2: Create `src/components/TagAutocomplete.tsx` per D-08, TAG-02.**

Props: `query: string`, `allTags: TagWithCount[]`, `entryTagIds: Set<string>`, `onSelect: (tag: TagWithCount) => void`, `onCreate: (name: string) => void`, `visible: boolean`.

If `!visible` or `query.trim() === ''`, return null.

Filter `allTags` by: name contains query (case-insensitive), AND tag id NOT in `entryTagIds` (don't suggest already-assigned tags).

Dropdown container: absolute positioned below the input, `bg-surface border border-border rounded-md shadow-md`, `max-h-[200px] overflow-y-auto`, `w-64`, `z-50`.

Each suggestion row: `px-3 py-2 flex items-center gap-2 cursor-pointer hover:bg-bg transition-colors`.
- Color swatch: 8x8px circle (`w-2 h-2 rounded-full`) with `style={{ backgroundColor: tag.color }}`
- Tag name: `text-body text-text`, flex-1
- Usage count: `text-label text-muted` right-aligned, e.g., "3"

Active item highlight: Track active index via keyboard navigation (arrow up/down). Active item gets `bg-bg`.

**"Create tag" row:** If the trimmed query does not exactly match any existing tag name (case-insensitive), show a final row: "Create tag '{query}'" in `text-body text-muted italic`. Clicking it calls `onCreate(query.trim())`.

Keyboard handling (this will be triggered from TagInput, but the logic lives here or is coordinated):
- ArrowDown: increment active index
- ArrowUp: decrement active index
- Enter: select the active item (or create if on the create row)
- Escape: close dropdown

**Step 3: Create `src/components/TagInput.tsx` per D-07, D-08, TAG-01.**

Props: `entryId: string`, `entryTags: TagWithCount[]`, `onTagAdded: () => void`.

State: `query` (string), `showDropdown` (boolean).

Input element: borderless (`border-none outline-none bg-transparent`), placeholder "Add tag..." in `text-muted`, `text-body`, `min-w-[120px]`. On focus: set `showDropdown = true`. On blur (with delay to allow dropdown click): set `showDropdown = false`.

On input change: update `query`, set `showDropdown = true`.

On Enter key: if autocomplete has an active selection, use that. If the typed name matches an existing tag exactly, assign it. Otherwise, create a new tag with `tagStore.createTag(query)` then assign it with `tagStore.addTagToEntry(entryId, newTag.id)`. Clear the input. Call `onTagAdded()`.

Render `<TagAutocomplete>` positioned below the input. Pass `allTags` from `useTagStore` `tags`, `entryTagIds` as a Set of the current entry's tag IDs, handlers for select and create.

When a tag is selected from autocomplete: call `tagStore.addTagToEntry(entryId, tag.id)`, clear input, call `onTagAdded()`.
When creating from autocomplete: call `tagStore.createTag(name)`, then `tagStore.addTagToEntry(entryId, newTag.id)`, clear input, call `onTagAdded()`.

**Step 4: Create `src/components/TagRow.tsx` per D-07.**

Props: `entryId: string`.

State: `entryTags: TagWithCount[]` — loaded via `tagStore.getEntryTags(entryId)` on mount and whenever `entryId` changes or after tag add/remove.

Container: `border-t border-border bg-bg px-8 py-3` (matching editor horizontal padding = px-8 = 32px). Flex row, `flex-wrap`, `gap-2` (8px), `items-center`.

Render existing tag pills: map `entryTags` to `<TagPill>` components. onRemove: call `tagStore.removeTagFromEntry(entryId, tag.id)`, then reload entry tags. onColorChange: call `tagStore.updateTagColor(tag.id, newColor)`, then reload entry tags.

At the end of the flex row, render `<TagInput entryId={entryId} entryTags={entryTags} onTagAdded={reloadEntryTags} />`.

Also load all tags on mount via `tagStore.loadTags()` so autocomplete has data.

**Relative positioning:** The TagRow container should have `relative` class so the autocomplete dropdown can be absolutely positioned within it.
  </action>
  <verify>
    <automated>cd C:/Users/Jason/Dev/MemoryLane && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/TagPill.tsx contains "color-mix" (tinted background per UI-SPEC)
    - src/components/TagPill.tsx contains "rounded-full" (pill shape)
    - src/components/TagPill.tsx contains "TAG_COLORS" (imports preset colors)
    - src/components/TagPill.tsx contains "Popover" (color picker popover on click)
    - src/components/TagPill.tsx contains "Check" (checkmark icon on selected color)
    - src/components/TagPill.tsx contains "group-hover:opacity-100" or similar (x button shows on hover)
    - src/components/TagAutocomplete.tsx contains "usage_count" or "usage" (shows usage count per TAG-02)
    - src/components/TagAutocomplete.tsx contains "Create tag" (create suggestion for new tags)
    - src/components/TagAutocomplete.tsx contains "max-h-[200px]" (scrollable dropdown per UI-SPEC)
    - src/components/TagInput.tsx contains "Add tag" (placeholder text)
    - src/components/TagInput.tsx contains "createTag" (creates new tags on Enter per TAG-01)
    - src/components/TagInput.tsx contains "addTagToEntry" (assigns tag to entry)
    - src/components/TagRow.tsx contains "border-t border-border" (top border separator per UI-SPEC)
    - src/components/TagRow.tsx contains "px-8" (matches editor horizontal padding)
    - src/components/TagRow.tsx contains "TagPill" and "TagInput" (sub-components)
    - src/components/TagRow.tsx contains "getEntryTags" (loads entry-specific tags)
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Tag row renders below the editor with colored pills, inline tag creation, autocomplete dropdown with usage counts, color picker popover with 8 presets, and tag removal via pill x button.</done>
</task>

<task type="auto">
  <name>Task 2: Build DeleteEntryDialog and wire TagRow + delete into editor and sidebar</name>
  <files>
    src/components/DeleteEntryDialog.tsx,
    src/components/EntryEditor.tsx,
    src/components/EntryListItem.tsx
  </files>
  <read_first>
    src/components/EntryEditor.tsx,
    src/components/EntryListItem.tsx,
    src/components/TagRow.tsx,
    src/stores/entryStore.ts,
    src/components/ui/alert-dialog.tsx,
    .planning/phases/02-editor-tags/02-UI-SPEC.md
  </read_first>
  <action>
**Step 1: Create `src/components/DeleteEntryDialog.tsx` per UI-SPEC Delete Entry Confirmation, EDIT-02.**

Props: `entryId: string`, `open: boolean`, `onOpenChange: (open: boolean) => void`.

Use shadcn `AlertDialog` with controlled open state (`open` and `onOpenChange` props).

Content per UI-SPEC copywriting contract:
- Title: "Delete this entry?"
- Description: "This entry will be permanently deleted. You can't undo this."
- Cancel button: shadcn secondary variant, label "Cancel"
- Confirm button: `bg-destructive text-white hover:bg-destructive/90` variant, label "Delete Entry"

On confirm: call `useEntryStore.getState().deleteEntry(entryId)`, then call `onOpenChange(false)`.

```tsx
import {
  AlertDialog, AlertDialogContent, AlertDialogHeader,
  AlertDialogTitle, AlertDialogDescription, AlertDialogFooter,
  AlertDialogCancel, AlertDialogAction
} from './ui/alert-dialog'
```

Style the AlertDialogAction button with destructive styling: `className="bg-destructive text-white hover:bg-destructive/90"`.

**Step 2: Update `src/components/EntryListItem.tsx` to add delete action.**

Add a delete icon (lucide `Trash2`, 14px) that appears on hover of the entry row. Position it at the far right of the row. Default: `opacity-0`. On row hover: `opacity-100` using `group` / `group-hover:opacity-100` pattern. Icon color: `text-muted` default, `hover:text-destructive` on icon hover.

Add local state `showDeleteDialog: boolean`. Clicking the trash icon sets it to true (and stops event propagation so the entry is not selected).

Render `<DeleteEntryDialog entryId={entry.id} open={showDeleteDialog} onOpenChange={setShowDeleteDialog} />`.

Important: The trash icon click must call `e.stopPropagation()` to prevent the row's onClick (selectEntry) from firing.

**Step 3: Update `src/components/EntryEditor.tsx` to render TagRow.**

Import `TagRow` from `./TagRow`. Replace the TagRow placeholder comment from Plan 01 with `<TagRow entryId={entryId} />`.

Place it after the editor content area (scroll container) and before the closing container div. The TagRow sits at the bottom of the editor pane, separated from editor content by its own top border.

Ensure the editor layout is: MetadataBar (top, from Plan 02) -> Editor scroll area (middle, flex-1) -> TagRow (bottom, fixed at bottom of pane).

The outer container should be `flex flex-col h-full` so that MetadataBar and TagRow are fixed and the editor content area scrolls between them.
  </action>
  <verify>
    <automated>cd C:/Users/Jason/Dev/MemoryLane && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/DeleteEntryDialog.tsx contains "Delete this entry?" (dialog title)
    - src/components/DeleteEntryDialog.tsx contains "permanently deleted" (dialog description)
    - src/components/DeleteEntryDialog.tsx contains "Delete Entry" (confirm button label)
    - src/components/DeleteEntryDialog.tsx contains "AlertDialog" (uses shadcn component)
    - src/components/DeleteEntryDialog.tsx contains "deleteEntry" (calls store delete on confirm)
    - src/components/DeleteEntryDialog.tsx contains "bg-destructive" (destructive button styling)
    - src/components/EntryListItem.tsx contains "Trash2" (delete icon)
    - src/components/EntryListItem.tsx contains "group-hover:opacity-100" or similar (icon appears on hover)
    - src/components/EntryListItem.tsx contains "stopPropagation" (prevents row click on delete)
    - src/components/EntryListItem.tsx contains "DeleteEntryDialog" (renders dialog)
    - src/components/EntryEditor.tsx contains "TagRow" (tag row rendered below editor)
    - src/components/EntryEditor.tsx contains "MetadataBar" (metadata bar rendered above editor, from Plan 02)
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Delete entry dialog with proper confirmation UX accessible from sidebar hover icon. TagRow wired into EntryEditor below the content area. Complete tag workflow: create, assign, remove, recolor, delete unused tags. Full entry lifecycle: create, edit, tag, delete.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. Manual: Launch app. Type in editor, then click below editor in the tag input. Type "personal" and press Enter — tag is created with auto-assigned color, pill appears. Type "work" — second tag appears with different color. Click a pill — color picker popover opens with 8 swatches, click one to change color. Hover a pill — x appears, click to remove tag from entry. Open autocomplete — shows tags with usage counts. Delete tag: usage_count 0 tags can be deleted.
4. Manual: Hover an entry in sidebar — trash icon appears. Click trash — confirmation dialog opens with "Delete this entry?" title. Click "Delete Entry" — entry is removed from list and editor switches to another entry.
</verification>

<success_criteria>
- Tag input below editor accepts new tag names and creates them on Enter (TAG-01)
- Autocomplete dropdown shows filtered suggestions with color swatch and usage count (TAG-02)
- "Create tag '{name}'" appears when no exact match exists
- Tag pills show with tinted color background from their assigned preset color (TAG-03)
- Clicking a pill opens a color picker popover with 8 swatches (TAG-03, D-09)
- Hovering a pill shows x to remove the tag from the entry
- Tags with 0 entries can be deleted (TAG-04)
- Sidebar entry rows show trash icon on hover, leading to delete confirmation dialog
- Delete dialog follows exact copywriting from UI-SPEC
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-editor-tags/02-03-SUMMARY.md`
</output>
